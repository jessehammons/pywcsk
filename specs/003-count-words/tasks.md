# Tasks: Count Words

## Tasks

- [x] T001 Add `count_words()` to `pywcsk/counter.py` and update `analyze()` to populate `words`
- [x] T002 Add unit tests for `count_words()` to `tests/test_counter.py` (empty, single, multi, spaces, tabs, no newline, whitespace-only, across lines)
- [x] T003 Add `-w` flag to `pywcsk/cli.py`; print `counts.words` when flag is set
- [x] T004 Create `tests/test_count_words.py` — CLI integration tests for `-w` via `CliRunner`
- [x] T005 Add oracle tests for word counting to `tests/test_oracle.py`
- [x] T006 Run full test suite and pre-commit; confirm all green
